import React from "react";
import "./Footer.css";

const Footer = React.memo(() => {
   return (
      <footer className="footer">
         <div>Done by Alexander Jacobson</div>
      </footer>
   );
});

export default Footer;
